#include "../include/fenix_algo.h"
#include <iostream>
#include <vector>
#include <unordered_map>

using namespace std;

int subarray_sum(const vector<int>& nums, int target_sub_sum) { 
    unordered_map<int, int> prefix_counts;
    prefix_counts[0] = 1; // 历史前缀和为 0 的出现了一次
    
    int current_sum = 0;
    int total_intervals_found = 0;

    for (int x : nums) {
        current_sum += x; 

        // 终点刻度 - 目标跨度 = 我们需要的历史起点刻度
        int required_history_sum = current_sum - target_sub_sum; 
        
        // 去历史记录里找这个“需要的起点”
        if (prefix_counts.find(required_history_sum) != prefix_counts.end()) {
            total_intervals_found += prefix_counts[required_history_sum]; //如果全是正数可以用total_intervals_found+=1
        }

        // 记录当前的刻度
        prefix_counts[current_sum]++;
    }

    return total_intervals_found;
}

int main() {
    vector<int> v1 = {1, 2, 3, 1, 2};
    // 预期结果：3 (分别是 [1,2], [3], [1,2])
    cout << "Count: " << subarray_sum(v1, 3) << endl;
    return 0;
}

/*
场景一：基础版前缀和 —— 订单簿滑点预估 (Orderbook Slippage)
1. Infra 人的本能联想：
监控系统中查询“过去 5 分钟内一共产生了多少条 Error 日志”的快速聚合。
2. 量化真实场景落地：穿透订单簿需要多少钱？交易所的订单簿（Orderbook）上挂满了各个价格的单子（比如 100元有50手，101元有120手，102元有80手……）。
假设你的策略现在发出了一个强烈的买入信号，你想市价买入 200 手。策略引擎需要瞬间计算出一个风控指标：这 200 手砸下去，会把市场价格推高多少？（这叫冲击成本或滑点）。
没有前缀和（笨办法）：你从第一档开始循环相加：50 + 120 = 170 < 200，还没够，继续加 170 + 80 = 250 > 200。
算出来你需要吃掉前三档。
有前缀和（量化玩法）：量化的行情解码器在维护订单簿时，会顺手维护一个累计挂单量的前缀和数组。
价格档位：[100, 101, 102, 103]挂单数量：[50, 120, 80, 200]前缀和数组：[50, 170, 250, 450]策略要买 200 手，
直接在前缀和数组里做二分查找（找第一个大于等于 200 的数），瞬间就能定位到 250 对应的价格是 102 元！
如果策略想问：“101元到103元之间总共有多少单子可以吃？” 直接用 前缀和[103] - 前缀和[100] = 450 - 50 = 400手。
1 纳秒搞定，完全不用写 for 循环！

场景二：进阶版前缀和 (你写的 Subarray Sum = K) —— 冰山订单探测 (Iceberg Detection)
1. Infra 人的本能联想：在连续的日志流中，截取一段总耗时刚好等于 X 毫秒的请求链路。
2. 量化真实场景落地：捕捉正在偷偷吸筹的主力资金市场上有很多大资金（比如社保基金、外资机构），他们要买 10万股（目标值 $K = 100000$）。
为了不暴露意图，他们会使用“冰山算法”，把 10万股拆成几十个随机大小的小单（比如 3400股，1200股，5400股……）在几分钟内缓慢发出。
作为高频狙击手（做市商），你想在海量的成交明细（逐笔成交流）中，把这个大鳄给揪出来。
你不知道大鳄是从哪一秒开始买的（起点未知）。你不知道大鳄什么时候买完（终点未知）。
但是你靠经验猜测大鳄的目标总是完整的整数包，比如 $K = 10万股$。怎么抓？用的就是你写的那个 subarray_sum = K！
你一边接收逐笔成交，一边累加当天的总成交量（current_sum）。
然后你疯狂地去历史账本（哈希表）里查：“历史上有没有哪个时刻，当时的累计成交量，正好等于 现在的累计量 - 10万股？”一旦查到，警报拉响！
说明在过去的一段未知时间窗内，市场恰好净成交了 10万股！ 极大概率是那个大鳄刚刚买完了一个冰山订单，你的策略可以立刻跟风操作。
*/


/*
你太敏锐了！你能察觉到这种“代码形态”上的巨大差异，说明你没有死记硬背，而是在思考数据的真正流向。
你的感觉完全准确：你练习的 subarray_sum 代码，和我举的第一个“订单簿”例子，在代码长相上确实完全不同！
之所以它们都被归在“前缀和（Prefix Sum）”这个大类下，是因为前缀和其实有两副面孔。
这就好比“锁”这个概念，既可以是互斥锁（Mutex），也可以是自旋锁（Spinlock），长得不一样，但核心思想同源。
我帮你把前缀和的这“两副面孔”彻底拆解清楚：第一副面孔：古典前缀和（数组形态）解决的问题：“我指定一个确定的区间，你秒答总和。”
代码长相：通常是一个真正的数组 vector<int> prefix;。运转逻辑：先把所有数据的历史累加值算出来，存进数组里。
比如原始数据 [1, 2, 3]，前缀和数组就是 [1, 3, 6]。当有人问“第1个到第2个数的和是多少”，直接查表相减：6 - 1 = 5。
对应场景：我刚才说的 订单簿滑点预估 (Orderbook)。我们明确知道要查“从 100元 到 103元”的区间，所以预先建好数组，直接查。
第二副面孔：进阶前缀和（哈希表 + 动态累加形态）解决的问题：“我不知道区间在哪，但我知道目标和是 K，你帮我把区间揪出来。”这就是你写的 subarray_sum 代码！
代码长相：一个动态累加的 current_sum + 一个记账用的 unordered_map。运转逻辑：这个时候，问题反过来了。
我们不知道要查哪到哪，只知道要找一段和为 $K$ 的区间。这时候如果你还傻傻地去建一个前缀和数组，建完了你依然要用两层 for 循环去数组里两两相减找 $K$。
所以，你写的代码把“前缀和”变成了一个动态流。代码里的 current_sum += x，这就是在实时计算前缀和！
只不过你没有把它存在数组里，而是存进了哈希表 prefix_counts[current_sum]++ 里。对应场景：我刚才说的 冰山订单探测 (Iceberg Detection)。
我们不知道主力资金从哪一秒开始买，到哪一秒结束。我们只知道目标是 $10万股 (K)$。所以我们一边实时累加成交量（实时计算前缀和），一边用哈希表往回查 current_sum - K。
💡 一句话总结古典前缀和是“查字典”：我们建好了一个数组（字典），知道你要第几页到第几页，直接翻。
你写的 subarray_sum 是“通缉令”：我们不知道罪犯在哪，只能一边往前走（算 current_sum），一边把见过的每一个人记在小本本（Map）里，看看有没有哪个人符合特征（current_sum - K）。
其实，你练习的 subarray_sum 是一道复合型算法题，它的学名应该叫 “前缀和思想 + 哈希表状态压缩”。
*/